This is a little school project I've been working on. As of now the project is at its beginning, but as I move forward I'll update the description.

To get started:

```bash
npm install
npm run serve-js
```

Then head over to localhost:8080.

